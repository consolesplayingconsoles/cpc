# Route packets between the Dreamcast's ppp0 link and the LAN uplink.
# DreamPi bridges the DC onto the LAN with proxyarp, so IPv4 forwarding
# must be on (off by default on trixie).
net.ipv4.ip_forward=1
