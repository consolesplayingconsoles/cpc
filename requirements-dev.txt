# CPC local DEV tooling — macOS dev machine ONLY. NOT for console nodes.
#
# This powers the "drive an emulator" feature (Robutek → Output → Local Emulator
# (Virtual Keyboard)): the KeyboardSink synthesizes controller input as keystrokes
# via pynput. Console nodes (PowerPC/ARM/MIPS) never run this — their runtime deps
# are the pure-stdlib set in pluto-python-tui/requirements.txt, and pynput is NOT
# deployed to them.
#
# Homebrew's python3 is externally-managed (PEP 668), so install into a venv:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements-dev.txt
#
# start-pluto-lab.sh auto-detects .venv. For the CLI use .venv/bin/python (or
# `source .venv/bin/activate` once, then plain `python`).
pynput==1.8.2

# The Claude-capture "eyes": pre-process each captured frame into structured state
# (brightness, where the light is, geometry, later OCR of on-screen text) so Claude
# reads facts not raw pixels. Capture host only, never the console nodes. ffmpeg (the
# frame grabber) and tesseract (optional, for OCR) are system packages, not pip:
#   brew install ffmpeg tesseract
opencv-python-headless==4.13.0
numpy
